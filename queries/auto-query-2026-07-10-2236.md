# Auto Query: 현재 Wiki에서 에이전트 실패는 '지식 상태 위반'(ADEMA)으로 아키텍처 차원에서 방지하거나, C/R(Crab, DeltaBox)로 사후 롤백하는 두 축으로 분석되는데, 본 논문이 노이즈가 많은 실행 추적에서 구조적 궤적 패턴을 분석해 실패의 인과적 근원을 추출하는 접근은 실패를 '방지·복구할 사건'이 아닌 '인과 지식으로 변환할 데이터'로 재정의하는 것인가?

**날짜**: 2026-07-10
**대상 논문**: From Noisy Traces to Root Causes: Structural Trajectory Analysis and Causal Extraction for Agent Optimization
**선택 이유**: 기존 Wiki가 에이전트 실패를 아키텍처적 방지와 상태 롤백의 두 축으로만 다루고 있는 것과 대조되어, 실패를 인과 진단의 학습 데이터로 전환하는 제3의 패러다임을 제시하기 때문이다.

## Wiki 답변

# 실패의 재정의: '방지·복구할 사건'에서 '인과 지식으로 변환할 데이터'로

질문하신 판단은 정확합니다. 이 논문("From Noisy Traces to Root Causes: Structural Trajectory Analysis and Causal Extraction for Agent Optimization")은 Wiki에 이미 축적된 에이전트 실패 처리의 두 축과 정면으로 대비되는 **제3의 축**을 제시합니다.

---

## 기존 두 축의 정리

Wiki에 등록된 실패 처리 접근은 다음과 같이 정리됩니다:

**축 1 — 아키텍처 차원의 사전 방지:** [[adema]]가 '지식 상태 오케스트레이션'을 통해 에이전트의 내부 지식 상태가 위반되기 전에 구조적으로 차단하는 경로입니다. 실패를 **발생하지 않아야 할 사건**으로 전제합니다.

**축 2 — 인프라 차원의 사후 롤백:** [[agentic-harness-engineering]]의 관점에서 [[agent-os-semantic-gap]]을 매핑한 [[algorithm-system-translation-gap]]의 구체적 현현이 바로 Crab의 C/R 런타임입니다. 에이전트의 의미론적 의존 그래프를 OS 수준의 체크포인트(전체 스냅샷)로 번역할 때 발생하는 비효율을 감수하더라도, 실패 후 **상태를 되돌리는 것**을 목표로 합니다. 실패를 **복구해야 할 사건**으로 전제합니다.

두 축 공통의 암묵적 전제: **실패는 처리(방지 또는 복구)의 대상이지, 그 자체로 가치를 가지는 대상이 아니다.**

---

## 본 논문의 제3축: 실패를 인과 지식으로 변환

본 논문은 이 전제를 근본적으로 뒤집습니다. 노이즈가 많은 실행 추적(noisy traces)에서 구조적 궤적 패턴을 분석하고, 실패의 인과적 근원(root cause)을 추출하는 접근은 다음과 같은 패러다임 전환을 의미합니다:

| | 축 1 (ADEMA) | 축 2 (Crab C/R) | **축 3 (본 논문)** |
|---|---|---|---|
| 실패의 본질 | 위반 사건 | 복구 사건 | **인과 데이터** |
| 시간적 관점 | 사전 차단 | 사후 롤백 | **추적 분석 (사후→미래)** |
| 산출물 | 정상 상태 유지 | 이전 상태 복원 | **인과 모델 (재사용 가능)** |
| 실패에 대한 태도 | 제거해야 할 것 | 되돌려야 할 것 | **학습해야 할 것** |

핵심은 본 논문이 reflection-based 메커니즘에서 LLM을 "진단자"로 활용하되, 단순한 반성이 아니라 **구조적 궤적 분석(structural trajectory analysis)**을 통해 실패에서 인과 구조를 추출한다는 점입니다. 이는 실패를 일회성 에러가 아닌, **재사용 가능한 인과 지식으로 변환하는 파이프라인**입니다.

---

## 새로운 개념적 연결

이 재정의는 Wiki의 기존 엔티티들과 다음과 같은 새로운 연결망을 생성합니다:

**1. [[agent-execution-semantic-opacity]]의 해소 경로로서의 인과 추출**
Wiki에 등록된 `agent-execution-semantic-opacity`는 에이전트 실행의 의미론적 불투명성을 지칭합니다. 기존에는 이 불투명성을 [[agent-os-semantic-gap]]에서 OS 레벨의 스냅샷으로 우회하려 했으나, 본 논문은 불투명성 그 자체를 **분석 대상**으로 삼아 인과 구조를 역추적함으로써 불투명성을 지식으로 전환합니다.

**2. [[causal-mechanistic-interpretability]]의 에이전트 실행 영역으로의 확장**
기존 `causal-mechanistic-interpretability`가 주로 모델 내부(activation, circuit)의 인과 메커니즘을 다루었다면, 본 논문은 이를 **에이전트의 다단계 실행 궤적**으로 확장합니다. 모델 내부의 인과성이 아닌, 행동-환경 상호작용의 인과성을 추출한다는 점에서 차이가 있습니다.

**3. [[adaptive-credit-granularity]]과의 구조적 보완**
`adaptive-credit-granularity`가 성공/실패에 대한 크레딧을 어느 세밀도로 할당할 것인지를 다룬다면, 본 논문은 그 크레딧 할당의 **인과적 근거**를 제공합니다. "어떤 단계가 실패의 원인인가"를 구조적 궤적 분석으로 입증함으로써, 크레딧 할당의 정당성을 인과적으로 뒷받침합니다.

**4. [[co-failure-ceiling]]에 대한 진단 도구로서의 가능성**
다중 에이전트 시스템에서 `co-failure-ceiling`이 존재한다면, 개별 에이전트의 노이즈 추적만으로는 근원을 찾기 어렵습니다. 구조적 궤적 분석이 에이전트 간 상호작용 패턴에서 인과적 근원을 추출할 수 있다면, co-failure의 구조적 원인을 진단하는 도구가 될 수 있습니다.

---

## 결론: 패러다임 전환의 의미

본 논문의 접근이 의미하는 바는 명확합니다. [[adema]]가 실패를 **아키텍처적 제약**으로, [[agentic-harness-engineering]]가 실패를 **인프라적 복구 대상**으로 취급한 것에 대해, 본 논문은 실패를 **에이전트 정책 개선을 위한 인과적 원료**로 재정의합니다.

이는 특히 [[recursive-self-improvement]]의 맥락에서 중요한데——자기 개선 루프가 단순한 출력 수정 수준을 넘어 구조적 인과 지식의 축적으로 이어지려면, 실패를 "되돌릴 것"이 아닌 "해석할 것"으로 대우하는 전환이 필수적이기 때문입니다. Crab의 C/R이 **상태 보존**의 문법이라면, 본 논문의 접근은 **상태 해석**의 문법을 제안하는 셈입니다.

## 핵심 통찰

실패를 '상태 보존(C/R)'의 대상에서 '상태 해석'의 대상으로 전환하여, 일회성 에러를 재사용 가능한 인과 지식으로 변환하는 파이프라인이 에이전트 자기 개선의 필수 조건이다.

---
_자동 Wiki Query로 생성됨_
