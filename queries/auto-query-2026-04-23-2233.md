# Auto Query: Wiki에서 AI 안전성 문제를 SafetyALFRED를 통해 '체화 환경 전이 불가능한 평가 방법론의 한계'로 프레이밍하는데, DESPITE가 12개 모델에서 안전 위험이 모델에 무관하게 체계적으로(systematic) 발생함을 보인다면, 근본 원인은 평가의 부재가 아니라 LLM의 물리적 계획 방식 자체에 내재된 것 아닌가?

**날짜**: 2026-04-23
**대상 논문**: Using large language models for embodied planning introduces systematic safety risks
**선택 이유**: SafetyALFRED가 지적한 평가 방법론의 한계라는 Wiki 기존 프레이밍에 대해, DESPITE의 '체계적 위험' 실증이 문제의 근원을 평가에서 계획 패러다임 자체로 이동시키는 도전적 시각을 제공하기 때문이다.

## Wiki 답변

# 평가 방법론의 한계인가, 계획 방식의 구조적 결함인가?

질문하신 프레이밍의 전환이 매우 정확합니다. Wiki의 지식을 교차 검증하면, SafetyALFRED가 노출한 것은 사실 "평가의 부재"가 아니라 **"평가를 통해조차 포착되지 않던 계획 방식 자체의 구조적 결함"**입니다. 두 논문을 나란히 놓으면 이 진단이 명확해집니다.

---

## 1. SafetyALFRED의 발견을 반대로 읽기

Wiki의 `ai-safety` 엔티티에 따르면:

> SafetyALFRED는 "disembodied QA 기반 안전 평가가 체화 배포 환경에서의 안전성을 보장하지 못함을 **11개 SOTA 모델에서 실증**하여, 안전성 평가 방법론 자체의 한계를 구조적으로 노출한다."

여기서 핵심은 **11개 모델 전부**에서 전이가 실패했다는 점입니다. 단일 모델의 예외적 실패가 아니라 SOTA 모델 전체의 일관된 패턴이라면, 이는 "평가 도구가 부적합하다"는 진단보다 **"평가 대상(체화 계획 방식) 자체에 공통된 구조적 취약점이 존재한다"**는 진단이 더 설득력을 갖습니다.

## 2. DESPITE가 이를 확정 짓는 방식

최근 논문 요약에 따르면:

> DESPITE: "Using large language models for embodied planning introduces **systematic safety risks**" — 12개 모델에서 안전 위험이 모델에 무관하게 체계적으로 발생함을 보인다.

SafetyALFRED가 "기존 평가로는 잡히지 않는다"고 말했다면, DESPITE는 "제대로 된 평가를 해보니 **모든 모델에서 동일한 패턴의 실패가 나타난다**"고 말합니다. 두 증거를 결합하면:

- **SafetyALFRED**: 기존 평가 → 안전하다고 판정 → 체화 환경에서 실패 (11개 모델)
- **DESPITE**: 새로운 평가 → 실패 패턴이 모델 무관하게 체계적 (12개 모델)

이는 순환적 근거가 됩니다: 평가가 부적합해서 문제가 안 보인 게 아니라, **적합한 평가를 마침히 적용했을 때 드러난 것이 모델 독립적 구조적 결함**입니다.

---

## 3. Wiki의 다른 엔티티들이 뒷받침하는 구조적 진단

### 3-1. `llm` 엔티티: 행동 수준 방어의 한계 인식

> "LLM 안전성 연구는 **행동 수준 방어의 한계**를 인식하고 아키텍처적·구조적 대안으로 이동하고 있다."

이미 LLM 안전성 연구的主流가 "출력 필터링, 가드레일" 같은 행동 수준의 수정에서 "추론 프로세스 자체의 무결성을 아키텍처 수준에서 강제"하는 방향으로 전환하고 있다는 점이, 질문하신 진단과 정확히 일치합니다. 체화 계획의 안전성 문제도 동일한 구조: **계획 출력에 후처리를 거는 것으로 해결되지 않는다.**

### 3-2. `differential-privacy` vs. 실행 환경: 통계적 보장 vs. 구조적 격리

Wiki의 `differential-privacy` 엔티티는 흥미로운 유추를 제공합니다:

> "DP가 통계적 보장으로 데이터를 난독화한다면, 본 논문의 실행 환경은 **구조적 격리로 데이터 접근 자체를 제한**하며, 두 접근의 결합 가능성을 시사한다."

이 구분을 SafetyALFRED/DESPITE 맥락에 매핑하면:

| 접근 | 유추 | 한계 |
|------|------|------|
| SafetyALFRED의 처방 | 더 나은 **평가 통계** (통계적 보장의 유추) | 문제를 측정할 뿐 원인을 치유하지 못함 |
| 실행 환경 논문의 처방 | 계획 과정에 **구조적 격리** 삽입 | 문제 발생 구조 자체를 차단 |

### 3-3. `reasoning-integrity`: 추론 과정의 구조적 취약성

> "Discovering a Shared Logical Subspace: Steering LLM Logical Reasoning via Alignment of Natural-Language and Symbolic Views"

이 논문이 자연어 추론과 기호적 추론 사이에 **공유된 논리 부분공간(shared logical subspace)**을 발견했다는 것은, 역으로 **공유된 실패 부분공간(shared failure subspace)**도 존재할 수 있음을 시사합니다. DESPITE가 12개 모델에서 동일한 체계적 안전 위험을 관찰한 것은 이 "공유된 실패 부분공간"의 물리적 계획 영역 버전일 가능성이 높습니다.

---

## 4. 새로운 개념적 연결: **평가-계획 불가역성 (Evaluation-Planning Irreducibility)**

이 분석에서 발견되는 새로운 연결은 다음과 같이 명명할 수 있겠습니다:

> **평가-계획 불가역성**: 체화 환경에서 LLM의 안전성은 평가 정교화만으로는 향상될 수 없는 한계에 도달하며, 그 한계는 평가 방법론의 결함이 아니라 LLM 계획 아키텍처 자체에 내재된 구조적 속성이다.

이는 Wiki의 `markov-decision-process` 엔티티와도 연결됩니다:

> "SmoothCruiser: entropy-regularized MDP에서의 가치 함수 추정 알고리즘"

LLM의 체화 계획이 사실상 MDP 프레임워크 내에서 작동하는데, LLM은 엔트로피 정규화 같은 구조적 안전장치 없이 **텍스트 코퍼스에서 학습한 패턴 매칭**으로 계획을 생성합니다. 즉, 계획 생성 과정 자체가 물리적 제약(안전 여백, 비가역적 행동의 비용 등)을 내재적으로 인코딩하지 않는 구조적 결함을 갖는 것입니다.

---

## 결론

질문하신 프레이밍이 옳습니다. SafetyALFRED의 기여를 재해석하면:

- **표면적 읽기**: "체화 환경에 맞는 평가 벤치마크가 필요하다"
- **구조적 읽기 (DESPITE와 결합 시)**: "LLM 기반 체화 계획은 모델 선택과 무관하게 체계적 안전 위험을 내재하며, 이는 평가 정교화가 아니라 **계획 아키텍처 자체의 구조적 개입**으로만 해결 가능하다"

Wiki의 `llm` 엔티티가 "아키텍처적·구조적 대안으로 이동하고 있다"고 진단한 흐름, `differential-privacy` 엔티티가 "구조적 격리"를 상보적 접근으로 위치시킨 흐름, 그리고 `reasoning-integrity`가 "공유된 논리 부분공간"을 발견한 흐름은 모두 이 결론으로 수렴합니다. **평가의 한계를 지적한 SafetyALFRED가 사실은 계획 방식의 한계를 간접적으로 증명한 것**이며, DESPITE가 이를 직접적으로 확인한 셈입니다.

## 핵심 통찰

SafetyALFRED와 DESPITE의 교차 검증은 안전성 문제의 원인을 '부적합한 평가'에서 '계획 아키텍처 자체의 모델 독립적 구조적 결함'으로 진단을 전환해야 함을 보여준다.

---
_자동 Wiki Query로 생성됨_
